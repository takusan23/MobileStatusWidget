<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/widget_mobile_data_usage_graph_image_view"
        android:layout_width="match_parent"
        android:layout_gravity="top"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:padding="5dp" />

    <Button
        android:id="@+id/widget_mobile_data_usage_graph_update_button"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="5dp"
        android:background="@drawable/widget_rounded_button_background"
        android:clickable="true"
        android:drawableStart="@drawable/refresh_black_24dp"
        android:drawableTint="@android:color/system_accent1_100"
        android:elevation="0dp"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:text="10.0GB"
        android:textColor="@android:color/system_accent1_100"
        android:translationZ="0dp" />

</FrameLayout>